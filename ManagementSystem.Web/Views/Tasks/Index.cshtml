@using PagedList.Mvc
@model PagedList.IPagedList<ManagementSystem.Web.ViewModels.TaskViewModel>

@{
    ViewBag.Title = "Tasks List";
}

<h3>@ViewBag.Title</h3>

<div class="row">
    @Html.DisplayFor(x => x)
</div>
@Html.PagedListPager(Model, Page => Url.Action("Index", new { Page }))